<h2>Last added posts</h2>

<% @posts.each do |post| %>
	<%= post.content %>
	<br/>
	author: <%= post.author %>
	<br/>
	<span style="font-size: 75%; color: #999">created at <%= post.created_at %></span>
	<br/>
	<a href="/details/<%= post.id %>">Comments</a>
	<hr/>
	<% end %>